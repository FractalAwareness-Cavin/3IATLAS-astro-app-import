Ubicación del asistente de KStars
=================================

Usa el paquete `KStars_quick-append_Win-Mac-Linux.zip` (o los scripts de `tools/kstars/`).
La entrada de una línea se mantiene en el import pack y puede regenerarse con
`tools/update_orbital_elements.py`.
