Emplacement de l'assistant KStars
=================================

Utilisez l'archive `KStars_quick-append_Win-Mac-Linux.zip` (ou les scripts dans `tools/kstars/`).
L'entrée une ligne est conservée dans l'import pack et peut aussi être régénérée avec
`tools/update_orbital_elements.py`.
