Posizione dell'helper KStars
============================

Usa il pacchetto `KStars_quick-append_Win-Mac-Linux.zip` (o gli script in `tools/kstars/`).
La riga singola è nel pacchetto di importazione e può essere rigenerata con
`tools/update_orbital_elements.py`.
